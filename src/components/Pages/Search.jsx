import React from "react";
import SearchForm from "../Search/SearchForm";
import ProfileShort from "../Search/ProfileShort";

const Search = () => {
  return (
    <div className="search">
      <SearchForm />
      <ProfileShort />
    </div>
  );
};

export default Search;
